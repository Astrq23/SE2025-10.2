import React from 'react';
import StakingDashboard from '../components/StakingDashboard';

/**
 * Staking View - Feature 1: Staking & Rewards System
 * Displays the staking dashboard where users can:
 * - Stake tokens to earn 12% APY
 * - Unstake tokens anytime
 * - Claim accumulated rewards
 * - View staking statistics
 */
const StakingView: React.FC = () => {
  return <StakingDashboard />;
};

export default StakingView;
